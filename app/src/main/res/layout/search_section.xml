<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/tv_section_title"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="16dp"
        android:text="Friends"
        android:textStyle="bold"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_section"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        tools:listitem="@layout/item_user"
        app:layout_constraintTop_toBottomOf="@id/tv_section_title"
        app:layout_constraintBottom_toTopOf="@id/btn_section_show_more"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_section_show_more"
        android:layout_width="wrap_content"
        android:padding="-6dp"
        android:layout_height="wrap_content"
        android:text="Show more"
        android:backgroundTint="@color/cq_gray"
        android:layout_margin="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:cornerRadius="8dp"
        app:layout_constraintTop_toBottomOf="@id/rv_section"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>